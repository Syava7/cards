const initState = {

};



export const newPasswordReducer = (state = initState, action: any): typeof initState => {
  switch (action.type) {
    case '' : {
      return {

      }
    }


    default: return state;
  }
};