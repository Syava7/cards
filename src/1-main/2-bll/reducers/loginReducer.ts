const initState = {

};



export const authReducer = (state = initState, action: any): typeof initState => {
  switch (action.type) {
    case '' : {
      return {

      }
    }


    default: return state;
  }
};