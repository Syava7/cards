const initState = {

};



export const profileReducer = (state = initState, action: any): typeof initState => {
  switch (action.type) {
    case '' : {
      return {

      }
    }


    default: return state;
  }
};